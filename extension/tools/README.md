# tools/ — tool directory (stub)

- `webmcp-bridge.js` — read document.modelContext tools + registered window.* functions
- `infer.js` — discover non-DOM functions, extract names/args/JSDoc, sourcemaps, Prompt-API
- `directory.js` — the aggregated, ranked tool directory
